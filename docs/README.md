# RAG система для ассистента сисадмина

Умная RAG система для создания ассистента системного администратора с использованием:
- **RAGatouille + ColBERT** для эффективного семантического поиска
- **Ollama gemma:2b** для генерации ответов на русском языке
- **Умный chunking** с учетом структуры документов

## Возможности

- 🧠 Семантический поиск с использованием ColBERT
- 📚 Автоматическое разбиение документов на оптимальные чанки
- 💬 Интерактивный чат-интерфейс
- 🔍 Поиск по документации Ubuntu
- 📖 Ссылки на источники информации
- 🎯 **LangChain компрессоры документов** для улучшения качества контекста (опционально)

## Установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Установите и запустите Ollama

```bash
# Установка Ollama (Linux)
curl -fsSL https://ollama.com/install.sh | sh

# Запуск Ollama сервера
ollama serve

# В другом терминале загрузите модель gemma:2b
ollama pull gemma:2b
```

### 3. Подготовьте данные

Убедитесь, что у вас есть файл `parsed.jsonl` с данными в формате:
```json
{"id": "...", "source_url": "...", "title": "...", "text": "...", "meta": {...}}
```

## Быстрый старт

### 1. Установите зависимости
```bash
pip install -r requirements.txt
```

### 2. Убедитесь, что Ollama запущен
```bash
ollama serve
# В другом терминале:
ollama pull gemma:2b
```

### 3. Постройте индекс, если их нет в .ragatouille/colbert/indexes
```bash
python main.py --mode build --data parsed.jsonl
```

### 4. Запустите чат интерфейс
frontend
```bash
streamlit run rag_chat_app/frontend/app.py
```
backend
```bash
uvicorn rag_chat_app.backend.app:app --reload --port 8000
```

## Использование

### Построение индекса

Сначала необходимо построить индекс ColBERT из ваших данных:

```bash
python main.py --mode build --data parsed.jsonl
```

Для пересоздания индекса:
```bash
python main.py --mode build --data parsed.jsonl --rebuild
```

**Примечание:** Построение индекса может занять некоторое время (10-30 минут в зависимости от объема данных и мощности компьютера).

### Интерактивный чат

После построения индекса запустите интерактивный режим:

```bash
python main.py --mode chat
```

Пример использования:
```
Вопрос: Как настроить Active Directory на Ubuntu?
```

### Тестовый режим

Для быстрого тестирования:

```bash
python main.py --mode test --query "Как сделать бэкап системы?"
```


## Структура проекта

```
.
├── benchmarks # 2 benchmarks
│   ├── benchmark_rag.csv
│   ├── benchmark_rag.py
│   ├── build_all_indexes.sh
│   └── build_indexes.log
├── code_data # parse/load/split prepare data
│   ├── add_dataset.py
│   ├── add_url.py
│   ├── parse_ubiuntu.py
│   ├── scraper.py
│   └── split_parsed.py
├── data
│   ├── dataset1.jsonl
│   ├── dataset2.jsonl
│   ├── dataset_upload # datatsets can be uploaded (commands.json make too small chunks)
│   │   ├── commands.json
│   │   └── dataset1.parquet
│   ├── parsed.jsonl
│   ├── parsed_part1.jsonl
│   ├── parsed_part2.jsonl
│   ├── parsed_part3.jsonl
│   ├── parsed_part4.jsonl
│   ├── parsed_part5.jsonl
│   └── urls.txt
├── docs
│   ├── COMPRESSION.md
│   ├── README.md
│   └── SETUP.md
├── RAG
│   ├── chunking.py
│   ├── document_compressor.py
│   ├── main.py
│   └── rag_system.py
├── rag_chat_app # app
│   ├── backend
│   │   ├── app.py
│   │   ├── models
│   │   │   └── rag_model.py
│   │   ├── requirements.txt
│   │   ├── routes
│   │   │   ├── chat.py
│   │   └── services
│   │       └── retrieval.py
│   ├── frontend
│   │   ├── app.py
│   │   └── requirements.txt
│   └── README.md
└── requirements.txt
```

## Настройка

### Параметры chunking

В файле `rag_system.py` можно настроить параметры разбиения:

```python
rag = SysAdminRAG(
    chunk_size=512,      # Размер чанка (в токенах)
    chunk_overlap=50     # Перекрытие между чанками
)
```

### Параметры Ollama

```bash
python main.py --ollama-url http://localhost:11434 --ollama-model gemma:2b
```

### Использование компрессоров документов

Компрессоры документов из LangChain помогают фильтровать и сжимать найденные документы перед передачей в LLM, улучшая качество ответов:

```bash
# С компрессией (рекомендуется для лучшего качества)
python main.py --mode chat --use-compression

# С настройкой порога схожести
python main.py --mode chat --use-compression --compression-threshold 0.8
```

**Как это работает:**
- После поиска документов через ColBERT, компрессор использует `EmbeddingsFilter` для дополнительной фильтрации
- Документы с низкой семантической схожестью с запросом отфильтровываются
- Это уменьшает шум в контексте и улучшает качество генерации ответов

**Параметры:**
- `--use-compression`: Включить компрессию документов
- `--compression-threshold`: Порог схожести (0.0-1.0, по умолчанию 0.76)
  - Более высокое значение = более строгая фильтрация (меньше документов)
  - Более низкое значение = более мягкая фильтрация (больше документов)

## Архитектура

1. **Chunking** (`chunking.py`):
   - Разбивает документы на семантически связанные части
   - Учитывает границы предложений и параграфов
   - Сохраняет метаданные для каждого чанка

2. **RAG System** (`rag_system.py`):
   - Использует RAGatouille с ColBERT для индексации
   - Выполняет семантический поиск по запросу
   - Интегрируется с Ollama для генерации ответов

3. **Main** (`main.py`):
   - CLI интерфейс для работы с системой
   - Режимы: build, chat, test

## Примеры вопросов

- "Как настроить Active Directory на Ubuntu?"
- "Как сделать бэкап системы?"
- "Как установить и настроить Bacula?"
- "Как настроить DNS на Ubuntu Server?"
- "Как использовать etckeeper для версионирования конфигов?"

## Troubleshooting

### Ошибка подключения к Ollama

Убедитесь, что Ollama запущен:
```bash
ollama serve
```

Проверьте доступность:
```bash
curl http://localhost:11434/api/tags
```

### Ошибка при построении индекса

- Убедитесь, что у вас достаточно RAM (ColBERT требует ~4-8GB)
- Проверьте формат данных в `parsed.jsonl`
- Убедитесь, что установлены все зависимости

### Медленный поиск

- Уменьшите количество возвращаемых результатов (`k` параметр)
- Используйте меньший размер чанков
- Убедитесь, что индекс построен корректно
